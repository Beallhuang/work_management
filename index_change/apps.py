from django.apps import AppConfig


class IndexChangeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "index_change"
    verbose_name = "指数转化"
    verbose_name_plural = "指数转化"

